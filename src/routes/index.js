import React from 'react'
import { BrowserRouter as Router, Link, Match, Miss, IndexRouter } from 'react-router'
import Template from '../containers/Template'

const createRoutes = () => {
    return (
        <div>
            <Template />
        </div>
    )
}


export default createRoutes